import Account from '../../utils/account';

describe('Account', () => {
  describe('methods', () => {
    describe('getDetails', () => {
      it('Returns the updated amount and currency of the API user', () => { });
    });
  });
});